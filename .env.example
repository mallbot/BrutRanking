# Configuración de BrutRanking
# Copia este archivo a .env y modifica los valores según tu entorno

# Puerto en el host (IMPORTANTE: verifica que no esté en uso)
# Usa: netstat -tuln | grep :3000
HOST_PORT=3000

# Puerto interno del contenedor (no cambiar a menos que sea necesario)
CONTAINER_PORT=3000

# Nombre del contenedor (cámbialo si ya tienes un contenedor con este nombre)
CONTAINER_NAME=brutranking-app

# Nombre de la red Docker (cámbialo si tienes conflictos)
NETWORK_NAME=brutranking-network

# Entorno de ejecución
NODE_ENV=production

# Rutas locales para volúmenes (rutas absolutas recomendadas en producción)
# Ejemplo: /opt/brutranking/data
DATA_PATH=./data
UPLOADS_PATH=./uploads
